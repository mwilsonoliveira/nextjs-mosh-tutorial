"use client";

function AddToCart() {
  return (
    <div>
      <button onClick={() => console.log("click")}>
        Adicionar ao carrinho
      </button>
    </div>
  );
}

export default AddToCart;
